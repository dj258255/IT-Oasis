---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import { projects } from '../data/projects';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const assetPath = (p: string) => `${base}/${p.replace(/^\//, '')}`;

const posts = (await getCollection('blog'))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 8);
---

<BaseLayout title="소개 — IT Oasis">
  <div class="fade-in-up">
    <div class="mb-10 sm:mb-14 px-1">
      <h1 class="text-4xl sm:text-5xl font-bold tracking-tight mb-5">
        <span class="gradient-text" data-ko="소개" data-en="About">소개</span>
      </h1>
      <p class="text-[20px] sm:text-[24px] text-gray-600 dark:text-gray-400 leading-relaxed max-w-lg">
        <span class="lang-ko">풍부한 상상과 호기심으로<br />다양한 프로젝트를 만들어갑니다.</span>
        <span class="lang-en hidden">Creating diverse projects<br />with rich imagination and curiosity.</span>
      </p>
    </div>

    <div class="glass-gradient rounded-2xl sm:rounded-3xl p-5 sm:p-7 md:p-10 relative overflow-hidden mb-14 sm:mb-20">
      <div class="absolute -top-20 -right-20 w-40 h-40 rounded-full bg-gradient-to-br from-accent/10 to-rose/10 blur-3xl pointer-events-none"></div>
      <div class="absolute -bottom-16 -left-16 w-32 h-32 rounded-full bg-gradient-to-br from-cyan/8 to-primary/8 blur-3xl pointer-events-none"></div>

      <div class="relative prose">
        <div class="lang-ko">
          <p>
            안녕하세요! 이 블로그를 운영하고 있는 개발자입니다.
          </p>
          <p>
            개발하면서 배운 것들을 기록하고 공유하기 위해 이 블로그를 시작했습니다.
          </p>

          <h2>기술 스택</h2>
          <ul>
            <li>이 블로그는 <strong>Astro</strong>와 <strong>Tailwind CSS</strong>로 만들어졌습니다.</li>
            <li><strong>GitHub Pages</strong>에서 호스팅됩니다.</li>
          </ul>

          <h2>연락처</h2>
          <ul>
            <li>GitHub: <a href="https://github.com">github.com</a></li>
          </ul>
        </div>
        <div class="lang-en hidden">
          <p>
            Hello! I'm the developer running this blog.
          </p>
          <p>
            I started this blog to record and share what I learn while developing.
          </p>

          <h2>Tech Stack</h2>
          <ul>
            <li>This blog is built with <strong>Astro</strong> and <strong>Tailwind CSS</strong>.</li>
            <li>Hosted on <strong>GitHub Pages</strong>.</li>
          </ul>

          <h2>Contact</h2>
          <ul>
            <li>GitHub: <a href="https://github.com">github.com</a></li>
          </ul>
        </div>
      </div>
    </div>

    <!-- Author profile -->
    <div class="author-card mb-14 sm:mb-20">
      <div class="flex flex-col sm:flex-row items-center sm:items-start gap-6 sm:gap-8 p-6 sm:p-8 md:p-10">
        <div class="w-28 h-28 sm:w-32 sm:h-32 rounded-full overflow-hidden shrink-0 bg-gradient-to-br from-primary/20 via-accent/15 to-rose/20">
          <img src={`${base}/avatar.png`} alt="Author" class="w-full h-full object-cover" onerror="this.style.display='none'" />
        </div>
        <div class="flex-1 min-w-0 text-center sm:text-left">
          <div class="flex items-center justify-center sm:justify-start gap-2.5 mb-3">
            <span class="text-[12px] sm:text-[13px] text-gray-400 dark:text-gray-500 tracking-wider uppercase">Written by</span>
            <span class="text-[12px] sm:text-[13px] font-bold px-2.5 py-0.5 rounded-lg bg-primary/10 text-primary dark:bg-primary-light/10 dark:text-primary-light">@IT Oasis</span>
          </div>
          <p class="text-[13px] sm:text-[14px] text-gray-500 dark:text-gray-400 leading-relaxed mb-5" data-ko="풍부한 상상과 호기심으로 다양한 프로젝트를 만들어갑니다." data-en="Creating diverse projects with rich imagination and curiosity.">
            풍부한 상상과 호기심으로 다양한 프로젝트를 만들어갑니다.
          </p>
          <div class="flex items-center justify-center sm:justify-start gap-2">
            <a href="https://github.com" target="_blank" rel="noopener" class="author-social-btn" aria-label="GitHub">
              <svg class="w-[18px] h-[18px]" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
              </svg>
            </a>
            <a href="mailto:contact@example.com" class="author-social-btn" aria-label="Email">
              <svg class="w-[18px] h-[18px]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75" />
              </svg>
            </a>
          </div>
        </div>
      </div>
    </div>

    <!-- Projects carousel -->
    <section class="mb-14 sm:mb-20">
      <div class="flex items-center justify-between mb-6 px-1">
        <h2 class="text-[18px] sm:text-[20px] font-bold tracking-tight flex items-center gap-2.5">
          <svg class="w-5 h-5 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
          </svg>
          <span data-ko="프로젝트" data-en="Projects">프로젝트</span>
        </h2>
        <div class="flex items-center gap-2">
          <button class="carousel-btn" data-carousel="projects" data-dir="prev" aria-label="Previous">
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" /></svg>
          </button>
          <button class="carousel-btn" data-carousel="projects" data-dir="next" aria-label="Next">
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" /></svg>
          </button>
        </div>
      </div>
      <div class="carousel-track" id="carousel-projects">
        {projects.map((project) => (
          <div class="carousel-item w-[280px] sm:w-[320px] shrink-0">
            <div class="glass-card rounded-2xl overflow-hidden h-full group">
              <div class="aspect-[16/10] bg-gradient-to-br from-gray-100 to-gray-50 dark:from-gray-800/50 dark:to-gray-900/50 relative overflow-hidden">
                {project.image && (
                  <img src={project.image} alt="" class="w-full h-full object-cover absolute inset-0 group-hover:scale-105 transition-transform duration-700" loading="lazy" onerror="this.style.display='none'" />
                )}
                <div class="w-full h-full flex items-center justify-center">
                  <svg class="w-10 h-10 text-gray-200 dark:text-gray-700" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="0.8">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
                  </svg>
                </div>
              </div>
              <div class="p-4 sm:p-5">
                <h3 class="text-[14px] sm:text-[15px] font-bold mb-1.5 group-hover:text-primary transition-colors duration-300">{project.title}</h3>
                <p class="text-[12px] sm:text-[13px] text-gray-500 dark:text-gray-400 leading-relaxed line-clamp-2 mb-3">{project.description}</p>
                <div class="flex flex-wrap gap-1">
                  {project.tags.map((tag) => (
                    <span class="text-[10px] font-medium px-2 py-0.5 rounded-md bg-gray-100 dark:bg-gray-800/50 text-gray-500 dark:text-gray-400">{tag}</span>
                  ))}
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- Latest stories carousel -->
    <section class="mb-8">
      <div class="flex items-center justify-between mb-6 px-1">
        <h2 class="text-[18px] sm:text-[20px] font-bold tracking-tight flex items-center gap-2.5">
          <svg class="w-5 h-5 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
          </svg>
          <span data-ko="최신 스토리" data-en="Latest Story">최신 스토리</span>
        </h2>
        <div class="flex items-center gap-2">
          <button class="carousel-btn" data-carousel="stories" data-dir="prev" aria-label="Previous">
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" /></svg>
          </button>
          <button class="carousel-btn" data-carousel="stories" data-dir="next" aria-label="Next">
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" /></svg>
          </button>
        </div>
      </div>
      <div class="carousel-track" id="carousel-stories">
        {posts.map((post) => (
          <a href={`${base}/blog/${post.id}`} class="carousel-item w-[260px] sm:w-[280px] shrink-0 block group">
            <div class="glass-card rounded-2xl overflow-hidden h-full">
              <div class="aspect-[16/10] bg-gradient-to-br from-gray-100 to-gray-50 dark:from-gray-800/50 dark:to-gray-900/50 relative overflow-hidden">
                {post.data.coverImage && (
                  <img src={assetPath(post.data.coverImage)} alt="" class="w-full h-full object-cover absolute inset-0 group-hover:scale-105 transition-transform duration-700" loading="lazy" onerror="this.style.display='none'" />
                )}
                <div class="w-full h-full flex items-center justify-center">
                  <svg class="w-10 h-10 text-gray-200 dark:text-gray-700" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="0.8">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
                  </svg>
                </div>
              </div>
              <div class="p-4">
                <h3 class="text-[13px] sm:text-[14px] font-semibold leading-snug mb-1.5 group-hover:text-primary transition-colors duration-300 line-clamp-2">{post.data.title}</h3>
                <p class="text-[11px] sm:text-[12px] text-gray-400 dark:text-gray-500">
                  {post.data.date.toLocaleDateString('ko-KR', { year: 'numeric', month: 'short', day: 'numeric' })}
                  <span class="mx-1 opacity-30">·</span>
                  {post.data.category}
                </p>
              </div>
            </div>
          </a>
        ))}
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .author-card {
    background: rgba(255, 255, 255, 0.45);
    border: 1px solid rgba(255, 255, 255, 0.5);
    backdrop-filter: blur(20px) saturate(1.4);
    -webkit-backdrop-filter: blur(20px) saturate(1.4);
    border-radius: 1.25rem;
    box-shadow: 0 2px 20px rgba(0, 0, 0, 0.04);
  }

  :global(.dark) .author-card {
    background: rgba(30, 30, 40, 0.45);
    border-color: rgba(255, 255, 255, 0.06);
    box-shadow: 0 2px 20px rgba(0, 0, 0, 0.15);
  }

  .author-social-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 12px;
    background: rgba(0, 0, 0, 0.06);
    color: #6b7280;
    transition: all 0.2s ease;
  }

  .author-social-btn:hover {
    background: rgba(56, 189, 248, 0.1);
    color: #0284c7;
    transform: translateY(-1px);
  }

  :global(.dark) .author-social-btn {
    background: rgba(255, 255, 255, 0.06);
    color: #9ca3af;
  }

  :global(.dark) .author-social-btn:hover {
    background: rgba(56, 189, 248, 0.12);
    color: #38bdf8;
  }

  .carousel-track {
    display: flex;
    gap: 1rem;
    overflow-x: auto;
    scroll-behavior: smooth;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    padding-bottom: 0.5rem;
  }

  .carousel-track::-webkit-scrollbar { display: none; }
  .carousel-track { -ms-overflow-style: none; scrollbar-width: none; }

  .carousel-item {
    scroll-snap-align: start;
  }

  .carousel-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 34px;
    height: 34px;
    border-radius: 12px;
    background: rgba(0, 0, 0, 0.03);
    border: 1px solid rgba(0, 0, 0, 0.06);
    color: #6b7280;
    transition: all 0.2s ease;
    cursor: pointer;
  }

  .carousel-btn:hover {
    background: rgba(56, 189, 248, 0.08);
    border-color: rgba(56, 189, 248, 0.15);
    color: #38bdf8;
  }

  :global(.dark) .carousel-btn {
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(255, 255, 255, 0.06);
    color: #9ca3af;
  }

  :global(.dark) .carousel-btn:hover {
    background: rgba(56, 189, 248, 0.1);
    border-color: rgba(56, 189, 248, 0.2);
    color: #7dd3fc;
  }
</style>

<script>
  function initCarousels() {
    document.querySelectorAll<HTMLButtonElement>('.carousel-btn').forEach((btn) => {
      btn.addEventListener('click', () => {
        const id = btn.dataset.carousel;
        const dir = btn.dataset.dir;
        const track = document.getElementById(`carousel-${id}`);
        if (!track) return;
        const scrollAmount = 300;
        track.scrollBy({ left: dir === 'next' ? scrollAmount : -scrollAmount, behavior: 'smooth' });
      });
    });
  }

  initCarousels();
  document.addEventListener('astro:page-load', initCarousels);
</script>
